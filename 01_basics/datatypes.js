"use strict"; //this line treats all JS code as newer version
//alert(3+3)->we are using nodeJS not browser, this one works in browser but not here
let name="Rini"// string 
let age=21// int
let isLoggedIn=false// bool
/*range
    numbers 2power53
    then you use bigInt
    null->standalone value
*/
let state=null
console.log(typeof null);//object
